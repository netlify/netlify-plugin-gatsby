{
  "name": "netlify-gatsby-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Function test",
  "author": "Matt Kane",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "HOST=0.0.0.0 gatsby develop",
    "start": "HOST=0.0.0.0 gatsby develop",
    "build": "lsb_release -a && uname -a && gatsby -v && gatsby build && ls -l .cache",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "build:netlify": "netlify build --offline",
    "preview": "netlify dev --offline",
    "test": "run-s build:netlify test:e2e",
    "test:e2e": "start-server-and-test preview 8888 test:jest",
    "test:jest": "jest",
    "plugin:install": "cd ../plugin/ && npm install",
    "plugin:build": "cd ../plugin/ && npm run build",
    "postinstall": "run-s plugin:install plugin:build"
  },
  "dependencies": {
    "@sindresorhus/slugify": "^1.1.2",
    "gatsby": "^3.11.0-alpha-qe-sm.92",
    "gatsby-source-filesystem": "^3.11.0-alpha-qe-sm.108",
    "gatsby-transformer-remark": "^4.8.0-alpha-qe-sm.108",
    "lmdb-store": "^1.6.7",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@netlify/functions": "^0.7.2",
    "co-body": "^6.1.0",
    "cookie": "^0.4.1",
    "download": "^8.0.0",
    "form-data": "^4.0.0",
    "fs-extra": "^10.0.0",
    "jest": "^27.0.0",
    "linkfs": "^2.1.0",
    "multer": "^1.4.2",
    "node-fetch": "^2.6.1",
    "npm-run-all": "^4.1.5",
    "path-to-regexp": "^6.0.0",
    "start-server-and-test": "^1.12.2",
    "tempy": "^1.0.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=14.15.0"
  }
}
